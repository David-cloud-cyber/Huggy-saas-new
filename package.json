{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "node server.js",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "test": "node scripts/ai-model-allowlist.test.mjs && node scripts/qa-static-checks.mjs && node scripts/platform-architecture.test.mjs && node scripts/platform-e2e-mock.test.mjs",
    "test:smoke": "node scripts/smoke-http-checks.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4",
    "vite": "^6.2.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
